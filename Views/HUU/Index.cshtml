@model IEnumerable<CUONGDB.Models.HUU>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HUUID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HUUName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HUUSđt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.HUUSL)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
             <td>
                @Html.DisplayFor(modelItem => item.HUUID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HUUName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HUUSđt)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.HUUSL)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HUUID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HUUID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HUUID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
